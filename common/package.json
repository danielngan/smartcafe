{
  "name": "smartcafe-common",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^16.18.68",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "clean": "rm -fr dist",
    "build": "yarn tsc",
    "clean-build": "yarn clean && yarn build"
  },
  "devDependencies": {
    "jest-html-reporter": "^3.10.2",
    "jest-html-reporters": "^3.1.7",
    "jest-mock-extended": "^3.0.5"
  }
}
